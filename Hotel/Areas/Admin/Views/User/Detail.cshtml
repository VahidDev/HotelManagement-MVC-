@model UserDetailViewModel
  <!-- Navbar -->
@section Navbar{
    <nav class="main-header navbar navbar-expand navbar-white navbar-light">
        <!-- Left navbar links -->
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
            </li>
            <li class="nav-item d-none d-sm-inline-block">
                <a href="index3.html" class="nav-link">Home</a>
            </li>
            <li class="nav-item d-none d-sm-inline-block">
                <a href="#" class="nav-link">Contact</a>
            </li>
        </ul>

        <!-- Right navbar links -->
        <ul class="navbar-nav ml-auto">
            <!-- Navbar Search -->
            <li class="nav-item">
                <a class="nav-link" data-widget="navbar-search" href="#" role="button">
                    <i class="fas fa-search"></i>
                </a>
                <div class="navbar-search-block">
                    <form class="form-inline">
                        <div class="input-group input-group-sm">
                            <input class="form-control form-control-navbar" type="search" placeholder="Search" aria-label="Search">
                            <div class="input-group-append">
                                <button class="btn btn-navbar" type="submit">
                                    <i class="fas fa-search"></i>
                                </button>
                                <button class="btn btn-navbar" type="button" data-widget="navbar-search">
                                    <i class="fas fa-times"></i>
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </li>

            <!-- Messages Dropdown Menu -->
            <li class="nav-item dropdown">
                <a class="nav-link" data-toggle="dropdown" href="#">
                    <i class="far fa-comments"></i>
                    <span class="badge badge-danger navbar-badge">3</span>
                </a>
                <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                    <a href="#" class="dropdown-item">
                        <!-- Message Start -->
                        <div class="media">
                            <img src="~/Admin/dist/img/user1-128x128.jpg" alt="User Avatar" class="img-size-50 mr-3 img-circle">
                            <div class="media-body">
                                <h3 class="dropdown-item-title">
                                    Brad Diesel
                                    <span class="float-right text-sm text-danger"><i class="fas fa-star"></i></span>
                                </h3>
                                <p class="text-sm">Call me whenever you can...</p>
                                <p class="text-sm text-muted"><i class="far fa-clock mr-1"></i> 4 Hours Ago</p>
                            </div>
                        </div>
                        <!-- Message End -->
                    </a>
                    <div class="dropdown-divider"></div>
                    <a href="#" class="dropdown-item">
                        <!-- Message Start -->
                        <div class="media">
                            <img src="~/Admin/dist/img/user8-128x128.jpg" alt="User Avatar" class="img-size-50 img-circle mr-3">
                            <div class="media-body">
                                <h3 class="dropdown-item-title">
                                    John Pierce
                                    <span class="float-right text-sm text-muted"><i class="fas fa-star"></i></span>
                                </h3>
                                <p class="text-sm">I got your message bro</p>
                                <p class="text-sm text-muted"><i class="far fa-clock mr-1"></i> 4 Hours Ago</p>
                            </div>
                        </div>
                        <!-- Message End -->
                    </a>
                    <div class="dropdown-divider"></div>
                    <a href="#" class="dropdown-item">
                        <!-- Message Start -->
                        <div class="media">
                            <img src="~/Admin/dist/img/user3-128x128.jpg" alt="User Avatar" class="img-size-50 img-circle mr-3">
                            <div class="media-body">
                                <h3 class="dropdown-item-title">
                                    Nora Silvester
                                    <span class="float-right text-sm text-warning"><i class="fas fa-star"></i></span>
                                </h3>
                                <p class="text-sm">The subject goes here</p>
                                <p class="text-sm text-muted"><i class="far fa-clock mr-1"></i> 4 Hours Ago</p>
                            </div>
                        </div>
                        <!-- Message End -->
                    </a>
                    <div class="dropdown-divider"></div>
                    <a href="#" class="dropdown-item dropdown-footer">See All Messages</a>
                </div>
            </li>
            <!-- Notifications Dropdown Menu -->
            <li class="nav-item dropdown">
                <a class="nav-link" data-toggle="dropdown" href="#">
                    <i class="far fa-bell"></i>
                    <span class="badge badge-warning navbar-badge">15</span>
                </a>
                <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                    <span class="dropdown-item dropdown-header">15 Notifications</span>
                    <div class="dropdown-divider"></div>
                    <a href="#" class="dropdown-item">
                        <i class="fas fa-envelope mr-2"></i> 4 new messages
                        <span class="float-right text-muted text-sm">3 mins</span>
                    </a>
                    <div class="dropdown-divider"></div>
                    <a href="#" class="dropdown-item">
                        <i class="fas fa-users mr-2"></i> 8 friend requests
                        <span class="float-right text-muted text-sm">12 hours</span>
                    </a>
                    <div class="dropdown-divider"></div>
                    <a href="#" class="dropdown-item">
                        <i class="fas fa-file mr-2"></i> 3 new reports
                        <span class="float-right text-muted text-sm">2 days</span>
                    </a>
                    <div class="dropdown-divider"></div>
                    <a href="#" class="dropdown-item dropdown-footer">See All Notifications</a>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link" data-widget="fullscreen" href="#" role="button">
                    <i class="fas fa-expand-arrows-alt"></i>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" data-widget="control-sidebar" data-slide="true" href="#" role="button">
                    <i class="fas fa-th-large"></i>
                </a>
            </li>
        </ul>
    </nav>
}
<!-- /.navbar -->
<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>DataTables</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a asp-controller="User" asp-action="Index">User</a></li>
                        <li class="breadcrumb-item active">Detail</li>
                    </ol>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <h3 class="card-title">User Details</h3>
                        </div>
                        <!-- /.card-header -->
                        <div class="card-body">
                            <table id="example2" class="table table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Username</th>
                                        <th>Email</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr class="userErrorInfoEl">
                                        <td>@Model.User.Id</td>
                                        <td>@Model.User.UserName</td>
                                        <td>@Model.User.Email</td>
                                        <td>
                                            <a class="btn btn-primary" asp-route-id="@Model.User.Id" asp-action="Update">Update</a>
                                            <!-- Button trigger delete modal -->
                                            <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#exampleModal">
                                                Delete
                                            </button>
                                            @if (Model.User.IsBlocked)
                                            {
                                                <button type="button" class="btn btn-dark unBlockModalBtn">
                                                    Unblock
                                                </button>
                                            }
                                            else
                                            {
                                                <!-- Button trigger block modal -->
                                                <button type="button" class="btn btn-dark BlockModalBtn">
                                                    Block
                                                </button>
                                            }
                                        </td>
                                        @if (!Model.User.IsHotelUser)
                                        {
                                            <td>
                                                <a class="btn btn-primary" asp-route-id="@Model.User.Id" asp-controller="AdminHotel" asp-action="CreateHotel">Make Hotel User</a>
                                            </td>
                                        }
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <!-- /.card-body -->
                    </div>
                    <!-- /.card -->
                </div>
                @if (Model.Reservations.Count != 0)
                {
                    <div class="col-12">

                        <div class="card-header">
                            <h3 class="card-title">Reservaion</h3>
                        </div>
                        <!-- /.card-header -->
                        <div class="card-body">
                            <table id="example2" class="table table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th>Hotel Name</th>
                                        <th>Room name</th>
                                        <th>Start date</th>
                                        <th>End date</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (Reservation reservation in Model.Reservations)
                                    {
                                        <tr>
                                            <td>@reservation.Room.Hotel.Name</td>
                                            <td>@reservation.Room.Name</td>
                                            <td>@reservation.StartDate.ToString("dd.MM.yyyy")</td>
                                            <td>@reservation.EndDate.ToString("dd.MM.yyyy")</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>

                        </div>
                    </div>
                }
                else
                {
                    <div class="card">
                        <!-- /.col -->
                        <div class="col-12">
                            <div class="text-danger">No Reservation</div>
                        </div>
                    </div>
                }
                <div class="card">
                    <!-- /.col -->
                    <div class="col-12">
                        @if (Model.User.Comments.Count == 0)
                        {
                            <span class="text-danger">No comments</span>
                        }
                        else
                        {
                            <div class="card">
                                <div class="card-header">
                                    <h3 class="card-title">Comments</h3>
                                </div>
                                <!-- /.card-header -->
                                <div class="card-body">
                                    <table id="example2" class="table table-bordered table-hover">
                                        <thead>
                                            <tr>
                                                <th>Hotel name</th>
                                                <th>Room name</th>
                                                <th>Content</th>
                                                <th>Rating</th>
                                                <th>Added date</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (Comment comment in Model.User.Comments)
                                            {
                                                <tr>
                                                    <td>@comment.Room.Hotel.Name</td>
                                                    <td>@comment.Room.Name</td>
                                                    <td>@comment.Content</td>
                                                    <td>
                                                        <ul class="hotel-stars text-warning" style="list-style:none;">
                                                            @{
                                                                int count = 0;
                                                                float rating = comment.Rating.Name;
                                                                bool isInteger = Int32.TryParse(rating.ToString()
                                                                    , out int result);
                                                                int intRating = (int)rating;
                                                            }
                                                            @for (int i = 0; i < intRating; i++)
                                                            {
                                                                count++;
                                                                <li style="display:inline-block;"><i class="fas fa-star"></i></li>
                                                            }
                                                            @if (!isInteger)
                                                            {
                                                                <li><i style="display:inline-block;" class="fas fa-star-half-alt"></i></li>
                                                                count++;
                                                            }
                                                            @while (count != 5)
                                                            {
                                                                count++;
                                                                <li><i style="display:inline-block;" class="far fa-star"></i></li>
                                                            }
                                                            @{
                                                                count = 0;
                                                            }
                                                        </ul>
                                                    </td>
                                                    <td>@comment.StartDate.ToString("dd.MM.yyyy")</td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>
                                <!-- /.card-body -->
                            </div><!-- /.card -->
                        }
                    </div>
                </div>
            </div> <!-- /.row -->
        </div>
        <!-- /.container-fluid -->
    </section>
    <!-- /.content -->
</div>
<!--Delete Modal-->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Delete User</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure that you want to delete this user? User: <span class="text-primary">@Model.User.UserName</span>
            </div>
            <input type="hidden" value="@Model.User.Id" id="userId" />
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary deleteCancel" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary" id="deleteUser">Delete</button>
            </div>
        </div>
    </div>
</div>
<!--Trigger block Moda-->
<input type="hidden" class="btn btn-danger" id="blockModalTriggerBtn" data-bs-toggle="modal" data-bs-target="#exampleModalBlock">
<!--Block modal-->
<div class="modal fade" id="exampleModalBlock" tabindex="-1" aria-labelledby="exampleModalLabelBlock" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabelBlock">Block User</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to block this user? User: <span class="text-primary" id="userInfoBlock">@Model.User.UserName</span>
                <input type="hidden" value="@Model.User.Id" id="userIdBlock" />
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-danger" data-bs-dismiss="modal" id="blockUser">Block</button>
            </div>
        </div>
    </div>
</div>
<!--Unblock modal-->
<!-- Real Button trigger the unblock modal -->
<input type="hidden" id="UnBlockModalActivate" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#exampleModalUnBlock">
<!--Unblock Modal -->
<div class="modal fade" id="exampleModalUnBlock" tabindex="-1" aria-labelledby="exampleModalLabelUnBlock" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabelUnBlock">Unblock User</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to unblock this user? User: <span class="text-primary" id="userInfoUnBlock">@Model.User.UserName</span>
                <input type="hidden" value="@Model.User.Id" id="userIdUnBlock" />
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-danger" data-bs-dismiss="modal" id="unBlockUserBtn">Unblock</button>
            </div>
        </div>
    </div>
</div>
@section Scripts{
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <script asp-append-version="true">
        "use strict";
        window.onload = () => {
            // For Deleting user
            jQuery(document).on("click", "#deleteUser", function (e) {
                $.ajax({
                    method: "Post",
                    url: "/Admin/User/Delete/" + $("#userId").val(),
                    success: function (res) {
                        if (res === "Success") {
                            window.location.replace("/Admin/User");
                        } else if (res === "Hotel Error") {
                            document.querySelector(".deleteCancel").click();
                            $(".userErrorInfoEl").append(`<span class="text-danger error">
                        First you need to delete the hotel of this user
                                   </span>`);
                            setTimeout(function () {
                                $(".error").detach();
                            }, 3 * 1000);
                        } else {
                            window.location.replace(window.location.href + "NotFound");
                        }
                    }
                });
            });
            // For Blocking User
            let BlockModalBtn = document.querySelector(".BlockModalBtn");
            if (BlockModalBtn != null) {
                BlockModalBtn.addEventListener("click", ActivateBlockModalBtn);
            }
            jQuery(document).on("click", "#blockUser", function (e) {
                $.ajax({
                    method: "Post",
                    url: "/Admin/User/Block/" + $("#userIdBlock").val(),
                    success: function (res) {
                        if (res) {
                            let el = $(".BlockModalBtn")[0];
                            $(el).removeClass("BlockModalBtn").addClass("unBlockModalBtn").text("Unblock");
                            el.removeEventListener("click", ActivateBlockModalBtn);
                            el.addEventListener("click", ActivateUnBlockModalBtn);
                        } else {
                            window.location.replace(window.location.href + "NotFound");
                        }
                    }
                });
            });
            // For UnBlocking
            let unBlockModalBtn = document.querySelector(".unBlockModalBtn");
            if (unBlockModalBtn != null) {
                unBlockModalBtn.addEventListener("click", ActivateUnBlockModalBtn);
            }
            jQuery(document).on("click", "#unBlockUserBtn", function (e) {
                $.ajax({
                    method: "Post",
                    url: "/Admin/User/UnBlock/" + $("#userIdUnBlock").val(),
                    success: function (res) {
                        if (res) {
                            let el = $(".unBlockModalBtn")[0];
                            $(el).removeClass("unBlockMobalBtn").addClass("BlockModalBtn").text("Block")
                            el.removeEventListener("click", ActivateUnBlockModalBtn);
                            el.addEventListener("click", ActivateBlockModalBtn);
                        } else {
                            window.location.replace(window.location.href + "NotFound");
                        }
                    }
                });
            });
        }
        function ActivateBlockModalBtn() {
            $("#blockModalTriggerBtn").click();
        }
        function ActivateUnBlockModalBtn() {
            $("#UnBlockModalActivate").click();
        }
    </script>

}